version: '3.8'

services:
  db:
    container_name: hl_database
    ports:
      - ${POSTGRES_PORT}:${POSTGRES_PORT}
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    build:
      dockerfile: ./db.Dockerfile
    restart: always
    volumes:
      - db:/var/lib/postgresql/data
    networks:
      - hl_network

  hl:
    container_name: home_library
    ports:
      - ${PORT}:${PORT}
    build:
      dockerfile: ./hl.Dockerfile
    env_file: .env
    restart: always
    depends_on:
      - db
    volumes:
      - .:/usr/src/home_library
      - node_modules:/usr/src/home_library/node_modules
    networks:
      - hl_network

volumes:
  db:
    name: hl_database_volume
  node_modules:
    name: hl_node_modules

networks:
  hl_network:
    external: false
    driver: bridge
